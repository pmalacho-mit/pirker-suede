{
  "name": "pirker-suede",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:rebuild": "FORCE_REBUILD=1 vitest",
    "release": "npm run --prefix release --"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.2",
    "vitest": "^4.1.0"
  }
}
